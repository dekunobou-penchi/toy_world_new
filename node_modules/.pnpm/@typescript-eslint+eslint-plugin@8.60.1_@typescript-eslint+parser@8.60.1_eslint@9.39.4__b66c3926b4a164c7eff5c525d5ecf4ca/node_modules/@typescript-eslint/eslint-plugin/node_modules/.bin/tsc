#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../typescript/bin/tsc" "$@"
else
  exec node  "$basedir/../../../../typescript/bin/tsc" "$@"
fi
# cmd-shim-target=/home/chicken/game/node_modules/.pnpm/@typescript-eslint+eslint-plugin@8.60.1_@typescript-eslint+parser@8.60.1_eslint@9.39.4__b66c3926b4a164c7eff5c525d5ecf4ca/node_modules/typescript/bin/tsc
